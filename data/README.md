# Seed data

Author against [docs/world.md](../docs/world.md).

`runtime/` is generated by the app and wiped by **Reset demo**. Do not commit live runtime files.

Do not write the full Acme answer into a single document. The agent has to find it.
